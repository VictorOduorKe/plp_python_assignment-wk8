# plp_python_assignment-wk8
procedure to run the project
 ```bash
 python3 -m venv venv #create a virtual environment
 source venv/bin/activate #activate virtual environment
pip install -r requirements.txt # install the libraries
```

running the covid 19 data analysis
```bash
python3 status.py --demo
```

running the streamlit in browser
```bash
streamlit run streamlit_app.py  #run streat and access it at in browser http://localhost:8500
